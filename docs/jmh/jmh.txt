帮我生成一个可以解析jmh结果results.json的本地html，可以用图标展现结果，要求如下：
只生成一个html，css和js包括在内；
页脚要注明Created by DeepSeek；
图表支持svg和canvas双格式切换；
results.json默认就在和html同目录，也支持文件拖拽方式上传resilts.json；
对于svg格式的图标，支持一键复制svg代码；
如果需要echarts，默认在../js目录下有echarts.min.js；
每个图标都有排序，同时也有个顶层排序按钮；
标题使用英语，代码内部注释可以用中文；
每个分组都可以折叠；
条形图可以设置竖着或是横着；
测试按照benchmark类分组，每个分组都会输出测试环境信息；
环境信息需要多一点；