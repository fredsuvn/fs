帮我生成一个可以解析benchmark(jmh)结果results.json的本地html，可以用图标展现结果，要求如下：
只生成一个html，css和js包括在内；
页脚要注明Created by [你的名字]；
图表支持svg和canvas双格式切换（包括全局和每个图表）；
results.json默认就在和html同目录，也支持文件拖拽方式上传results.json(但是拖拽默认折叠)；
每个svg格式的图表，支持一键复制svg代码；
如果需要echarts，默认在../js目录下有echarts.min.js；
每个图标都有排序，同时也有个顶层排序按钮；
排序目前只需要按照分数排序；
标题使用英语，代码内部注释可以用中文；
每个分组都可以折叠；
图表可以设置竖条形图或是横条形图；
测试按照benchmark类分组，每个分组都会输出测试环境信息；
每个图表的数据列都会包括errors等详细列；
每个测试分数对应的测试方法除了有方法名，还要有参数名（如涉及）；