plugins {
  id 'kitva'
  id 'java-platform'
  id 'kitva-publish'
}

description = "BOM of Gek."

javaPlatform {
  allowDependencies()
}

dependencies {
  constraints {
    //api project(":boat")
    file("${rootDir}").eachDirMatch(~/gek-(?!(plugins|draft)).*/) { dir ->
      api project(":${dir.name}")
    }
  }
}