package space.sunqian.fs.object.convert;

import space.sunqian.annotation.Nonnull;
import space.sunqian.annotation.Nullable;
import space.sunqian.annotation.RetainedParam;
import space.sunqian.fs.base.number.NumFormatter;
import space.sunqian.fs.base.option.Option;
import space.sunqian.fs.base.option.OptionKit;

final class ConvertBack {

    // static @Nonnull Option<?, ?> @Nonnull [] mergeOptions(
    //     @Nonnull Option<?, ?> @Nonnull @RetainedParam [] defaultOptions,
    //     @Nullable ConvertOption.DatePattern datePattern,
    //     @Nullable ConvertOption.NumPattern numPattern
    // ) {
    //     if (datePattern == null) {
    //         if (numPattern == null) {
    //             return defaultOptions;
    //         } else {
    //             NumFormatter numFormatter = ConvertKit.getNumFormatter(numPattern.value());
    //             return OptionKit.mergeOption(defaultOptions, ConvertOption.numFormatter(numFormatter));
    //         }
    //     } else {
    //         if (numPattern == null) {
    //
    //         } else {
    //
    //         }
    //     }
    // }

    private ConvertBack() {
    }
}
